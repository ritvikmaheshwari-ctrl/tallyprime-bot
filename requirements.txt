# TallyPrime Entry Prep Bot Python dependencies
# Install with:
#   python -m pip install -r requirements.txt
#
# Notes:
# - This is the lightweight/default install for normal use.
# - Tesseract OCR and Poppler are still external Windows installs.
# - EasyOCR is intentionally NOT installed here because it is heavy on 8 GB RAM PCs.
#   Install requirements-ocr-optional.txt only if you need the extra OCR fallback.

pandas>=2.2,<3
openpyxl>=3.1,<4
xlrd>=2.0,<3
pillow>=10,<12
pdf2image>=1.17,<2
pypdf>=4,<6
